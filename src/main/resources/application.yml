spring:
  application:
    name: javazolotaya

  datasource:
    url: jdbc:postgresql://localhost:5432/itmo_cities
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update

  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql

fibonacci:
  useCache: false